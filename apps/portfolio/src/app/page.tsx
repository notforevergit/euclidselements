/**
 * PHASE 6 — Carlos: this is the portfolio site.
 *
 * It stays deliberately empty until the game is real, because the strongest
 * version of this page is a case study about something that exists. The
 * outstanding facts (employers, dates, education, links) go here.
 */
export default function Home() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-24">
      <h1 className="font-display text-5xl font-extrabold tracking-tight">
        Carlos Arroyave
      </h1>
      <p className="mt-4 text-lg text-ink-muted">
        Software engineer. Laravel and MySQL by day, local inference clusters and
        Euclid by night.
      </p>
      <p className="mt-8 font-mono text-xs uppercase tracking-[0.14em] text-ink-faint">
        Placeholder &middot; built out in Phase 6
      </p>
    </main>
  );
}
